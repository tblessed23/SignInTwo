<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.msbs.android.signintwo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SignInTwo">
        <activity android:name=".stories.EditAudioDetailsActivity"></activity>
        <activity android:name=".stories.SavedAudioActivity" />
        <activity android:name=".GooeyActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".view.LoggedInActivity" />
        <activity android:name=".LoginRegisterActivity" />
        <activity android:name=".view.UserDisplayDetailsActivity" />
        <activity android:name=".view.UserEditDetailsActivity" />
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>